{% import "@quartiers_solidaires/macros.html.twig" as macros %}

<a
  data-toggle="collapse"
  href="#collapse-{{ key }}"
  class="card-pill {{ not is_open ? "collapsed" }}"
  role="tab"
  aria-expanded="{{ is_open ? "true" : "false" }}"
  aria-controls="collapse-{{ key }}"
  id="request{{ request.nid.value }}"
>
  <span class="card-pill-icon">
    {{ macros.icon('theme_' ~ request.field_theme.entity.field_sharing_icon.value) }}
  </span>

  <span class="card-pill-title d-flex">
    <span>{{ request.field_theme.entity.name.value }}</span>

    {# Request date display on small device and upper #}
    <span class="size-date d-none d-sm-inline mr-3 mr-md-0">
      <time datetime="{{ request.changed.date|date('Y-m-d') }}">
        {{ request.changed.date|bamboo_i18n_format_date('default_medium_date_only') }}
      </time>
    </span>
  </span>

  <span
    class="btn btn-outline-info btn-icon btn-icon-to-circle shadow-to-bottom btn-white ml-auto"
    aria-label="{{ 'qs.readmore.about'|t }} {{ request.label }}"
  >
    {{ macros.icon('look') }}
    <span class="d-none d-sm-inline">
      {{ 'qs.event.readmore'|t }}
      <span class="sr-only">{{ 'qs.event.readmore.about'|t }} {{ request.label }}</span>
    </span>
  </span>

</a>
