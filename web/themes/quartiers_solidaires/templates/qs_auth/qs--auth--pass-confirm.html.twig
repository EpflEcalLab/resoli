{% import "@quartiers_solidaires/macros.html.twig" as macros %}

<div class="d-flex flex-column justify-content-between">
  <div class="fullpage-wrapper py-4">

    <div class="container text-center mt-5 mb-5">
      {{ macros.icon('wait-lg', ['icon-xl']) }}
    </div>

    <div class="container mt-5 mb-auto text-center">
      <p class="lead">{{ 'qs.auth.pass.confirmation.thanks'|t }}</p>
      <p class="lead">{{ 'qs.auth.pass.confirmation.description'|t }}</p>
    </div>

    <div class="container text-center">
      <div class="row mb-3">
        <div class="col-6 mx-auto">
          <a class="btn btn-block btn-icon btn-icon-right btn-outline-primary btn-outline-invert" href="{{ path('qs_auth.login') }}">
            {{ 'qs_auth.login'|t }}
              {{ macros.icon('plus') }}
          </a>
        </div>
      </div>
    </div>
  </div>
</div>

