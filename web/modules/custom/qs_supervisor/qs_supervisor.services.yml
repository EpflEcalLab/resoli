services:
  qs_supervisor.auth_redirect:
    class: Drupal\qs_supervisor\EventSubscriber\AccountRedirect
    arguments: ['@current_route_match', '@current_user']
    tags:
      - {name: event_subscriber}
