services:
  qs_subscription.subscription_manager:
    class: Drupal\qs_subscription\Service\SubscriptionManager
    arguments: ['@current_user', '@entity_type.manager', '@database', '@cache_tags.invalidator', '@qs_acl.privilege_manager', '@plugin.manager.mail']
