services:
  # Event subscrbiber to avoid access of somes Taxonomy Terms
  qs_site.entity_access:
    class: Drupal\qs_site\EventSubscriber\EntityAccess
    arguments: ['@current_route_match']
    tags:
      - {name: event_subscriber}
