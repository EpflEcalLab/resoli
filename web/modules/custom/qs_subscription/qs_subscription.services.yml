services:
  qs_subscription.subscription_manager:
    class: Drupal\qs_subscription\Service\SubscriptionManager
    arguments: ['@current_user', '@entity_type.manager', '@entity.query', '@database', '@cache_tags.invalidator']
