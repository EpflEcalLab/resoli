services:
  # Redirect
  qs_default_content.generate_volunteerism:
    class: Drupal\qs_default_content\EventSubscriber\GenerateVolunteerismSubscriber
    arguments:
      - '@entity_type.manager'
    tags:
      - { name: event_subscriber }

  qs_default_content.user:
    class: Drupal\qs_default_content\EventSubscriber\UserSubscriber
    arguments:
      - '@entity_type.manager'
    tags:
      - {name: event_subscriber}
